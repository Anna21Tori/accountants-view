import { styled } from "styled-components";

export const Currency = styled.span`
                            font-size: 12px;
                            opacity: 1;
                            font-size: 12px;
                            font-weight: 400;
                            font-style: normal;
                            letter-spacing: 0px;
                            line-height: 16px;
                        `